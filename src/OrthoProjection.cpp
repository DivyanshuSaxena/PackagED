#include "Classes.h"

Point * OrthoProjection::possibleNeighbours(Point point) {
    ///
    /// Function that returns a list of possible neighbors of the point passed in parameter, from the current orthographic projection
    ///
}
